# bfs frontier trace

### graph:
```
ExplicitGraph(
    nodes={'S', 'A', 'B', 'G'},
    edge_list=[('A', 'B'), ('S', 'G'), ('S', 'A'), ('B', 'G')],
    starting_nodes = ['S'],
    goal_nodes = {'G'}
    )
```

### frontier trace
```
+s
-s
+sg
+sa
-sg
```